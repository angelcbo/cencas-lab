package com.cenicast.lis.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder for future JPA customisation (custom converters, naming strategy overrides, etc.).
 * Tenant filter application is handled by TenantFilterAspect, not here.
 */
@Configuration
public class JpaConfig {
}
